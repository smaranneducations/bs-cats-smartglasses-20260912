# Contract package

This package will host shared contracts used by services and workers.

Planned next files:

- object contracts
- MCP payload schemas
- publish job contracts
- render manifest schema

Keep schemas backward compatible where possible and version every contract.

# Architecture artifacts

The source artifact `platform-overview.architecture.json` and generated `platform-overview.html` visualize the current platform. The source is authoritative for diagram updates; generated HTML is a review artifact.

The normative written architecture is `docs/V1_SYSTEM_SPECIFICATION.md`. The observed release state is `docs/V1_RELEASE_READINESS.md`. A diagram cannot establish runtime completion.
